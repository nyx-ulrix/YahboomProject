Flask==2.3.3
paho-mqtt==1.6.1
python-dotenv==1.0.0
paramiko
numpy
scipy
# Optional: MobileCLIP-S1 backend text-label decode only (VIT_ENABLE_MODEL=true).
# Image-to-image matching runs in the browser, so these are NOT needed for the
# default image-to-image flow.
torch
open_clip_torch